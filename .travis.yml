language: generic
sudo: required
services:
  - docker

before_install:
  - docker build -t shvimal/docker-react -f Dockerfile.dev .

script:
  - docker run -e CI=true shvimal/docker-react npm run test -- --coverage